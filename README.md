# crytpo

A Telegram bot project built with DreamAgent.

## Prerequisites

- Python 3.10+

## Setup

```bash
python -m venv .venv
source .venv/bin/activate  # On Windows: .venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env  # Then fill in your Telegram token
python main.py
```

## Environment Variables

See `.env.example` for required variables. Copy it to `.env` and fill in your values.

## License

This project was generated by DreamAgent.
